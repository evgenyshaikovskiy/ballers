<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8" />
  <style>
    body {
      width: 100vw;
      height: 100vh;
      background-color: antiquewhite;
      display: flex;
      align-items: center;
      flex-direction: column;
    }

    .index-league-title {
      font-weight: bold;
      font-size: xx-large;
    }

    .leagues-wrapper {
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      gap: 10px;
    }

    .submit-form-btn {
      width: 100px;
      height: 50px;
      font-weight: 700;
    }

    .submit-form-btn:hover {
      cursor: pointer;
    }
  </style>
  <title>All leagues</title>
</head>

<body>
  <div class="index-league-title">All leagues</div>
  <div class="leagues-wrapper">
    {{#each leagues}}
    <div class="league-entity-wrapper">
      <div class="league-name">{{name}}</div>
      <div class="league-entity-actions">
        <form action="http://localhost:3000/league/delete/{{name}}" method="get">
          <button class="submit-form-action">Delete league</button>
        </form>
        <form action="http://localhost:3000/league/view/{{name}}" method="get">
          <button class="submit-form-action">View league</button>
        </form>
      </div>
    </div>
    {{/each}}
  </div>
  <form action="http://localhost:3000/league/create" method="get">
    <button class="submit-form-btn">Create new league</button>
  </form>
</body>

</html>