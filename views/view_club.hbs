<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8" />
  <style>
    body {
      width: 100vw;
      height: 100vh;
      background-color: antiquewhite;
      display: flex;
      align-items: center;
      flex-direction: column;
    }

    .index-club-title {
      font-weight: bold;
      font-size: xx-large;
    }

    .form-field {
      display: flex;
      justify-content: center;
      flex-direction: column;
      gap: 10px;
    }

    .form-label {
      font-weight: 500;
    }

    .form-input {
      outline: 0;
    }

    .clubs-wrapper {
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      gap: 10px;
    }

    .submit-form-btn {
      width: 100px;
      height: 50px;
      font-weight: 700;
    }

    .submit-form-btn:hover {
      cursor: pointer;
    }
  </style>
  <title>View club</title>
</head>

<body>
  <div class="club-wrapper">
    <form action="http://localhost:3000/club/update/{{club.name}}" method="post" class="club-edit-form">
      <div class="form-field">
        <div class="form-label">Enter new club name</div>
        <input type="text" name="name" placeholder="{{club.name}}" id="name" class="form-input" />
      </div>
      <button class="submit-form-btn">Edit</button>
    </form>
  </div>
</body>

</html>