<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8" />
  <style>
    body {
      width: 100vw;
      height: 100vh;
      background-color: antiquewhite;
      display: flex;
      align-items: center;
      flex-direction: column;
    }

    .index-league-title {
      font-weight: bold;
      font-size: xx-large;
    }

    .form-field {
      display: flex;
      justify-content: center;
      flex-direction: column;
      gap: 10px;
    }

    .form-label {
      font-weight: 500;
    }

    .form-input {
      outline: 0;
    }

    .leagues-wrapper {
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      gap: 10px;
    }

    .submit-form-btn {
      width: 100px;
      height: 50px;
      font-weight: 700;
    }

    .submit-form-btn:hover {
      cursor: pointer;
    }
  </style>
  <title>View league</title>
</head>

<body>
  <div class="league-wrapper">
    <form action="http://localhost:3000/league/update/{{league.name}}" method="post" class="league-edit-form">
      <div class="form-field">
        <div class="form-label">{{league.name}}</div>
        <input type="text" name="name" placeholder="{{league.name}}" id="name" class="form-input" />
      </div>
      <button class="submit-form-btn">Edit</button>
    </form>
  </div>
</body>

</html>